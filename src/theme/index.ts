/**
 * Theme exports
 *
 * Centralized theme configuration for the application.
 * This follows React Native best practices for theme organization.
 *
 * Structure:
 * - colors.ts: All color definitions (semantic, currency, overlay, etc.)
 * - typography.ts: Font and text styling definitions
 * - index.ts: Main export file (this file)
 *
 * Usage:
 * ```ts
 * import { Colors, SemanticColors, CurrencyColors } from '@/theme';
 * ```
 */

export * from './colors';
export * from './typography';

